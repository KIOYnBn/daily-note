```R
getwd()

# 读取文件
setwd("path")
data_1 <- read.table("txt文件", header=T, sep="\t")
data_2 <- read.csv("csv文件")
load("rdata文件")

# 写入文件
write.table(data_1.file="11.txt", row.names=F, sep="\t")
write.csv(data_2, file="22.csv", row.names = F)
save(exp, file="123.rdata")
```
