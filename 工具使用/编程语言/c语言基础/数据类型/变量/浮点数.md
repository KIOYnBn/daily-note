* 类型
	* double： 默认， 8字节
	* float： 4字节
* c语言中， 默认的小数类型为double
```c
#include <stdio.h>

int main(void) {
	double a = 10.34;
	float b = 10.34F;
	printf("double: %.2f, float: %.2f", a, b);
	printf("科学计数法:%e, 十六进制: %a", b, b);
	return 0;
}
```
