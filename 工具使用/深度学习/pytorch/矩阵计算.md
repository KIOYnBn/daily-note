# 数学原理
1. 点积概念
2. 特殊矩阵
	1. 正交矩阵
	2. 正定矩阵
	3. 对称矩阵
	4. 置换矩阵
3. 范数概念
	1. L1范数
	2. L2范数（F范数）
---
# 代码实现
```python
{
# 置换
newTensor = theTensor.T

# 克隆
newTensor = oldTensor.clone()

# 求和
theTensor.sum(axis=0, keepdims=True)

# 点积
torch.dot(x, y)

# 矩阵和向量相乘
torch.mv(A, x)

# 矩阵和矩阵相乘
torch.mm(A, B)

# L2范数
torch.norm(theTensor)

# L1范数
torch.abs(theTensor).sum()

# 累计和
theTensor.cumsum(axis=0)
}
```